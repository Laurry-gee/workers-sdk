{
	"name": "template-worker-router",
	"version": "0.0.0",
	"private": true,
	"scripts": {
		"deploy": "wrangler deploy index.js",
		"dev": "wrangler dev index.js",
		"test": "vitest"
	},
	"dependencies": {
		"itty-router": "^5.0.18"
	},
	"devDependencies": {
		"vitest": "^0.31.0",
		"wrangler": "^3.0.0"
	}
}
